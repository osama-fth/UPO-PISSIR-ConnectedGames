{
  "name": "edge-gateway",
  "version": "1.0.0",
  "description": "Edge Gateway Node per la Connected Games Platform — gestisce autenticazione OIDC, eventi MQTT e buffer offline SQLite",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "mqtt": "^5.10.0",
    "openid-client": "^5.7.1",
    "uuid": "^11.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
